name: Bundle SVG Emojis

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-and-bundle:
    runs-on: ubuntu-latest

    steps:
      - name: 🧩 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Install dependencies (if needed)
        run: |
          npm install  # or skip if you don’t have package.json

      - name: 🚀 Run SVG Bundler
        run: |
          node ultra_optimized_svg_bundler.js

      - name: 📤 Upload bundled output
        uses: actions/upload-artifact@v4
        with:
          name: emoji-data
          path: emojiData.js
